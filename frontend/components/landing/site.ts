/**
 * Constantes do site público.
 *
 * `CONTACT` é o destino de todo CTA de "falar com a equipe". Hoje aponta para o
 * e-mail do operador — trocar pelo comercial quando existir um, ou por uma rota
 * de formulário se o backend passar a receber leads.
 */
export const CONTACT = 'inovacoes@trustsis.com'
export const CONTACT_HREF = `mailto:${CONTACT}?subject=Demo%20do%20Rift`
