# Dependencies
node_modules/

# Next.js build output
.next/
.next-staging/
out/

# Environment variables (NUNCA commitar)
.env
.env.local
.env.production
.env.*.local

# Runtime data (dados reais de engagements e usuários — não versionar)
backend/data/

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# TypeScript build info
*.tsbuildinfo

# Build artifacts
dist/

# Reports gerados localmente
relatorio-full.html

# PM2
.pm2/

# GitHub Actions self-hosted runner
actions-runner/

# Backups de env (contêm segredos)
*.bak

# Config local do Claude Code (settings.local.json contém permissões/estado da máquina)
.claude/

# Artefatos de scratch / design — não são fonte da aplicação
graphify-out/
design_handoff_rifit_v3/
/Login Page v4.html

# Frameworks de BACKUP mantidos só em disco (oráculo de regressão). Não versionados
# no repo principal p/ evitar o gitlink de submódulo quebrado. O tronco consolidado
# (pentest-framework-v2-next) É versionado.
Agentes-Pentest/pentest-framework-v2/
Agentes-Pentest/pentest-framework-3/

# Relatórios de pentest gerados (contêm dados de alvo real — NUNCA versionar)
Relatorio-Pentest-*.html

# Core dumps
core.[0-9]*
